# Token de una Private App de HubSpot (Settings > Integrations > Private Apps)
# NUNCA se expone al frontend: solo lo usa el backend en server/.
HS_TOKEN=pat-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Puerto del backend proxy (opcional, default 3001)
PORT=3001
